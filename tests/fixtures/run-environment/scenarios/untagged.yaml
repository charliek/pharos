version: 1
id: env.untagged
name: no allowedEnvironments tag at all
service: s
tags: [read]
mode: compare_live
steps:
  - id: get
    request: { method: GET, path: /ok }
    compare: { strategy: json_semantic, status: same }
